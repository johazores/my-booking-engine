{
  "name": "sf-booking-infrastructure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.20.0 <25"
  },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "test": "node --test scripts/*.test.mjs src/server/auth/*.test.ts src/server/authorization/*.test.ts src/server/availability/*.test.ts src/server/bookings/*.test.ts src/server/branding/*.test.ts src/server/customers/*.test.ts src/server/inventory/*.test.ts src/server/memberships/*.test.ts src/server/organizations/*.test.ts src/server/pricing/*.test.ts src/server/tenancy/*.test.ts src/server/users/*.test.ts",
    "test:database": "node scripts/run-database-tests.mjs",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "db:migrate": "prisma migrate dev",
    "db:deploy": "prisma migrate deploy",
    "db:status": "prisma migrate status",
    "db:drift": "prisma migrate diff --from-schema prisma/schema.prisma --to-config-datasource --exit-code",
    "validate": "npm run typecheck && npm run lint && npm run test && npm run prisma:validate && npm run build",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "7.10.0",
    "@prisma/client": "7.10.0",
    "dotenv": "17.4.2",
    "next": "16.3.3",
    "pg": "8.23.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "eslint": "^9.0.0",
    "eslint-config-next": "16.3.3",
    "prisma": "7.10.0",
    "typescript": "6.0.3"
  }
}
